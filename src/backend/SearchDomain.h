#pragma once
#include "Config.h"

namespace backend
{
	enum SearchDomain
	{
		searchDomainAll,
		searchDomainOwn,
		searchDomainFavorites,
		searchDomainMax, // * Must be the last enum constant.
	};
}
